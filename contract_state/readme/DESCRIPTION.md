Adds a document workflow state to contracts: **Draft**, **Active** and
**Cancelled**.

A contract only generates invoices while it is active. Draft is for a contract
still being prepared internally — the invoicing button is hidden, the recurring
cron passes it over, and the customer does not see it in the portal.
